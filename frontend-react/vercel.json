{
    "rewrites": [
        {
            "source": "/api/:path*",
            "destination": "https://<BACKEND_HOST>/api/:path*"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}